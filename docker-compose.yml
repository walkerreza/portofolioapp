version: '3.8'

services:
  web:
    build: .
    container_name: portfolio-web
    ports:
      - "8080:80"
      - "3000:3000"
    restart: unless-stopped


    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80"]
      interval: 30s
      timeout: 10s
      retries: 3


